const initialState = {
  sellerStore: {},
};

export const storeReducer = (state = initialState, action) => {
  switch (action.type) {
    case "SET_STORE":
      break;

    default:
      return state;
  }
};
